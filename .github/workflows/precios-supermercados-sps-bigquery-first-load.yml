name: Precios SPS - Primera carga BigQuery (legado retirado)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Ruta productiva retirada
        run: echo "bigquery_first_load_retired_use_turso"
